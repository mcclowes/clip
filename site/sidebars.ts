import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  docs: ['index', 'register-tools', 'define-capabilities', 'generate-skills', 'commands', 'community-schemas', 'command-reference', 'clip-and-mcp'],
};

export default sidebars;
